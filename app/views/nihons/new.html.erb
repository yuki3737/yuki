<%= stylesheet_link_tag 'new', :media => "all" %>
<div class="post-container-n">
<p class="title">投稿フォーム</p>
<%= form_for @nihon do |f| %>
  
 
  <% end %>

  <%= form_with(model: @nihon, url: nihons_path, local: true) do |f| %>
    <p>
      <%= f.label :body,"投稿" %>
      <%= f.text_field :body %>
      <br>
      <%= f.label :content,"内容" %>
      <%= f.text_field :content %>
    </p>
    
    <div class="field">
  <%= f.label :picture %>
  <%= f.file_field :picture %>
  </div>
    <p>スペースを入力することで複数のタグを付けることができます。</p>
    <%= f.text_field :tag_name, placeholder: "投稿タグをつける" %>
    <%= f.submit %>
  <% end %>
</div>

  <div class="link">
  <%= link_to "一覧に戻る", nihons_path %>
  </div>
  

  